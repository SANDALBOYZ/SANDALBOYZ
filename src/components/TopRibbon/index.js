import React, { useState } from 'react';
import styled from 'styled-components';

const TopRibbon = () => {
  return (
    <div>
      Labor Day Flash Sale - 24 hours only! Subscribe to our newsletter to get
      access.
    </div>
  );
};

export default TopRibbon;
